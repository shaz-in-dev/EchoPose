# EchoPose Level-Up Master Plan (Top-Tier Maturity, Non-Copy)

This plan maps your requested capabilities into execution phases.

## Requested Capability Matrix

Status keys:
- DONE: foundation implemented in repo
- NEXT: started, needs implementation depth
- LATER: planned milestone
- EXTERNAL: requires GitHub/org settings outside source code

### Engineering and Product

- Self-learning / contrastive AI: NEXT
- Cross-environment generalization: NEXT
- WASM / browser support: LATER
- Disaster response mode (WiFi-Mat style): LATER
- 542+ tests: LATER
- Published pip package: DONE (scaffold)

### Repository Structure

- `v1/` legacy + proof system: DONE (scaffold)
- `docker/` release structure: DONE (scaffold)
- `docs/` + ADR system: DONE (scaffold)
- `vendor/` vendored crates strategy: LATER
- `data/` baselines + proofs: DONE (scaffold)
- `echopose_sdk/` package: DONE (scaffold)

### Ecosystem/Distribution

- Published crates on crates.io: LATER
- Docker images published: NEXT (workflow added)
- GitHub Pages site: NEXT
- Discussions community: EXTERNAL

## Phase 1 (Weeks 1-3): Credible Baseline

1. Stabilize CI and tests to green from fresh clone.
2. Publish first `echopose-sdk` pre-release to TestPyPI/PyPI.
3. Publish container images to GHCR on release tags.
4. Add docs index + ADR index + architecture limitations page.

Success gate:
- New contributor can run tests and start stack in under 20 minutes.

## Phase 2 (Weeks 4-8): Differentiation Engine

1. Implement contrastive pretraining on CSI windows.
2. Add domain-shift scoring and quality endpoint (`/quality`).
3. Add cross-room benchmark protocol and baseline report in `data/baselines`.
4. Add uncertainty-aware pose output fields.

Success gate:
- Cross-room benchmark report generated by script and committed.

## Phase 3 (Weeks 9-14): Platform Depth

1. Start Rust workspace split for aggregator internals.
2. Extract first crate and publish to crates.io.
3. Add tactical incident timeline and replay synchronization.
4. Add initial WASM module for browser-side validation utilities.

Success gate:
- At least 1 published crate and 1 WASM utility integrated in UI build.

## Phase 4 (Weeks 15-24): Scale and Community

1. Increase tests from current baseline to 150+, then 300+, then 542+.
2. Add disaster-response mode with scenario-driven tests.
3. Launch GitHub Pages documentation site.
4. Enable Discussions and publish contributor playbook.

Success gate:
- 542+ tests with passing CI and documented quality gates.

## Build Order for Your Exact Targets

Recommended strict order:
1. CI reliability
2. Package and image publishing
3. Benchmark-backed novelty (contrastive + cross-room)
4. Rust crate modularization
5. WASM/browser runtime
6. Community and docs growth

This order minimizes wasted effort and increases visible credibility fastest.

## Risks and Controls

- Risk: over-claiming before validation.
  - Control: mark experimental features explicitly and link every claim to a script.
- Risk: architecture sprawl.
  - Control: ADR required for every major new module.
- Risk: test count inflation without quality.
  - Control: maintain test quality gates (coverage, mutation targets later).

## Immediate Commands for You

1. Enable repository secrets: `PYPI_API_TOKEN`.
2. Create first release tag to test workflows.
3. Add `mkdocs.yml` and publish docs to GitHub Pages.
4. Start implementing contrastive pretraining in `inference/research/`.
