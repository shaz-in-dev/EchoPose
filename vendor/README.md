# Vendor Policy

This directory is reserved for pinned third-party code snapshots when reproducibility requires vendoring.

Rules:
- Prefer upstream package managers first.
- Vendor only when reproducibility or security review requires it.
- Keep LICENSE files and source origin metadata with each vendored component.
